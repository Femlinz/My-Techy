//Math and strings
let resullt = "5" + 5;
console.log(resullt); // "55"

//boolean
console.log(10 > 5 && "Cat" === "Dog"); console.log(10 > 5 || "Cat" === "Dog");

//if/else
    let score = 75;
    if (score >= 50) {
        console.log("You passed!");
    }else {
        console.log("You failed!");
    }

    //loops
    for (let i = 1; i <=3; i++) {
        console.log(i); 
    }
    //Strings length
    /**
     * How do you find the number of letters in a string named word?
     *  
     * 
     */
    let word  = "String";
    console.log(word.length); 

    let digit = "08107038130"
console.log(digit.length) 
